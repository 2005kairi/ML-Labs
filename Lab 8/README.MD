I loaded the KNN_Project_Data dataset and explored it using a pairplot to visualize relationships between features and the TARGET CLASS. I standardized all feature variables using StandardScaler to ensure equal influence during distance calculations. After splitting the data into training and testing sets, I trained a K‑Nearest Neighbors classifier starting with k=1 and evaluated its performance using predictions, a confusion matrix, and a classification report. I then tested multiple k values using the elbow method to identify the lowest error rate, retrained the model with the best k, and compared the improved results.
